<!DOCTYPE html>
<html lang="en">
<head>
   <title>Print House UK | Products</title>
   <%- include('../partials/header'); %>
   <link href="/css/finance.css" type="text/css" rel="stylesheet">
</head>
<body>
  <!--NAVBAR-->
  <%- include('../partials/navbar'); %>

  <!--HERO IMAGE AND TEXT-->
  <div class="hero-image d-flex justify-content-center align-items-center">
    <img src="/images/Finance-hero-img.png" class="img">
    <div class="hero-text-container">
    <h1 class="hero-text text-responsive">FINANCE</h1>
    <p class="hero-text">We’ll tailor a financing plan around you and your precise needs so that your business can continue going from strength to strength and achieving the aims you’ve set.</p>
    <div class="learn-more-btn">
      <a href="#products" class="learn-more">
        <span>Learn more </span>
      </a> 
     </div> 
    </div>
  </div>

  <!--BRAND CAROUSEL-->
  <%- include('../partials/brand-carousel'); %>

  <!--FOOTER-->
  <%- include('../partials/footer'); %>



  <script>
 /***brand swiper js**/ 
  var swiperOptions = {
  loop: true,
  freeMode: true,
  spaceBetween: 40,
  grabCursor: true,
  slidesPerView: 7,
  loop: true,
  autoplay: {
    delay: 1,
    disableOnInteraction: true
  },
  freeMode: true,
  speed: 5000,
  freeModeMomentum: false
};
var swiper = new Swiper(".swiper-container", swiperOptions);

/******Scroll top button*****/
let calcScrollValue = () => {
  let scrollProgress = document.getElementById("progress");
  let progressValue = document.getElementById("progress-value");
  let pos = document.documentElement.scrollTop;
  let calcHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
  let scrollValue = Math.round((pos * 100 / calcHeight));

 if(pos > 100){
  scrollProgress.style.display = "grid";
 } else {
  scrollProgress.style.display = "none";
 }

 scrollProgress.addEventListener("click", () => {
  document.documentElement.scrollTop = 0;
 });
 scrollProgress.style.background = `conic-gradient(#6479fe ${scrollValue}%, white ${scrollValue}%)`;
};

window.onscroll = calcScrollValue;
window.onload = calcScrollValue;
   
  </script>
</body>
</html>